
import "../styles/components/navbar.sass"

export const NavBar = () => {
  return (
    <div id="navbar">
      <a href="#">Home</a>
      <a href="#">Projetos</a>
      <a href="#">Contato</a>
    </div>
  )
}
